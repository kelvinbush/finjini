{
  "name": "finjini",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "start:compiled": "node dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:drop": "drizzle-kit drop",
    "docker:build": "docker build -t finjini .",
    "docker:build:dev": "docker build -f Dockerfile.dev -t finjini:dev .",
    "docker:run": "docker run -p 8080:8080 --env-file .env finjini",
    "docker:run:dev": "docker run -p 8080:8080 --env-file .env -v $(pwd)/src:/app/src finjini:dev",
    "docker:compose": "docker-compose up",
    "docker:compose:build": "docker-compose up --build",
    "docker:compose:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.7.0",
    "drizzle-kit": "^0.31.5",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/swagger": "^9.5.2",
    "@fastify/swagger-ui": "^5.2.3",
    "@neondatabase/serverless": "^1.0.2",
    "@paralleldrive/cuid2": "^2.2.2",
    "@types/ws": "^8.18.1",
    "bufferutil": "^4.0.9",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.6",
    "fastify": "^5.6.1",
    "fastify-plugin": "^5.1.0",
    "fastify-raw-body": "^5.0.0",
    "pino": "^10.0.0",
    "pino-pretty": "^13.1.1",
    "ws": "^8.18.3"
  }
}